export enum PrivateRoutes {
  HOME = "/profile",
  LOGOUT = "/auth/logout",
  DUMMY = "/dummy/check-private",
  USERS = "/users",
}

export enum PublicOnlyRoutes {
  LOGIN = "/login",
  REGISTER = "/register",
}
